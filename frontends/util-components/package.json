{
  "name": "util-components",
  "packageManager": "yarn@3.2.0",
  "main": "build/index.js",
  "devDependencies": {
    "typescript": "^4.7.4"
  },
  "peerDependencies": {
    "react": "*"
  },
  "scripts": {
    "postinstall": "./webpack_if_prod.sh",
    "start:dev": "tsc --watch",
    "build": "tsc",
    "build:flowtypes": "yarn global:build:flowtypes",
    "lint": "echo todo: set up lint",
    "fmt": "echo todo: set up fmt",
    "fmt:check": "echo todo: set up fmt:check",
    "scss_lint": "echo todo: set up scss_lint",
    "test": "yarn global:test",
    "test:watch": "yarn global:test:watch",
    "typecheck": "tsc --noEmit"
  }
}

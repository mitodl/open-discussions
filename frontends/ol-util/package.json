{
  "name": "ol-util",
  "packageManager": "yarn@3.2.0",
  "main": "./build/index.js",
  "devDependencies": {
    "@types/styled-components": "^5.1.25"
  },
  "peerDependencies": {
    "react": "^16.10 || 17 || 18",
    "styled-components": "5"
  },
  "scripts": {
    "postinstall": "yarn build",
    "start:dev": "yarn global:start:dev",
    "build": "yarn global:build",
    "clean": "yarn global:clean",
    "compile": "yarn global:compile",
    "compile:watch": "yarn global:compile:watch",
    "coverage": "yarn global:coverage",
    "fmt-check": "yarn global:fmt-check",
    "fmt-fix": "yarn global:fmt-fix",
    "lint-check": "yarn global:lint-check",
    "lint-fix": "yarn global:lint-fix",
    "test": "yarn global:test"
  }
}

{
    "name": "open-discussions-root",
    "private": true,
    "workspaces": [
        "frontends/*"
    ],
    "scripts": {
        "start:dev": "yarn workspace ${DEV_WORKSPACE:-'open-discussions'} run start:dev",
        "build": "yarn workspaces foreach run build",
        "lint": "yarn workspaces foreach run lint",
        "fmt": "yarn workspaces foreach run fmt",
        "fmt:check": "yarn workspaces foreach run fmt:check",
        "scss_lint": "yarn workspaces foreach run scss_lint",
        "fmt:flow": "yarn workspaces foreach run flow",
        "test": "yarn workspaces foreach run test"
    },
    "version": "0.0.0",
    "packageManager": "yarn@3.2.0"
}
